/** Étape 4 — zone tableau (placeholder : cadre plein ; détection géométrique future). */
export function detectTableRegionFullFrame(): { method: 'full_frame'; confidence: number } {
  return { method: 'full_frame', confidence: 0.55 }
}
